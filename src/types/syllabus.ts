
export interface SiteSyllabusResponse {
    items: Syllabus[],
    entityReference: string,
    entityURL: string,
    entityTitle: string

    redirectUrl: string,
    siteId: string,
}


export interface Syllabus {
}